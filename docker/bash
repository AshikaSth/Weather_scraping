#!/bin/sh
cd "$(dirname "$0")/.."
# Use docker compose service name instead of hardcoded container names
# Makes the script portable and works even if project name or container suffix changes
SERVICE=${1:-web_dashboard}  # default matches your docker-compose service

echo "Opening shell in $SERVICE..."
docker compose exec "$SERVICE" /bin/bash